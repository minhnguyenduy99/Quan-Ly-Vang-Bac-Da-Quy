<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:UIProject.Templates.CustomDataTemplates">
    <DataTemplate x:Key="ProductItemDataTemplate">
        <Border x:Name="OuterBorder"
                BorderThickness="0"
                BorderBrush="Transparent"
                Width="200"
                VerticalAlignment="Stretch"
                Margin="10">
            <StackPanel VerticalAlignment="Stretch"
                        Orientation="Vertical">
                <Image Source="{Binding ModelData.ImageSource}"
                       Height="70"
                       Stretch="Uniform"/>
                <Label Style="{DynamicResource SmallDefinedLabelStyle}">
                    <TextBlock TextWrapping="NoWrap"
                               Text="{Binding ModelData.ProductName}"/>
                </Label>
                <StackPanel Orientation="Horizontal">
                    <Label Style="{StaticResource SmallDefinedLabelStyle}"
                           FontWeight="Bold"
                           Content="{Binding ModelData.Price}"/>
                    <Label Style="{DynamicResource SmallDefinedLabelStyle}"
                           FontWeight="Bold"
                           Content="{Binding ModelData.Quantity}"/>
                </StackPanel>
            </StackPanel>
        </Border>
        <DataTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="true">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                                             Storyboard.TargetName="OuterBorder"
                                             From="0.5" To="1"
                                             Duration="0:0:0.4"/>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                                             Storyboard.TargetName="OuterBorder"
                                             From="1" To="0.5"
                                             Duration="0:0:0.4"/>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
        </DataTemplate.Triggers>
    </DataTemplate>
</ResourceDictionary>